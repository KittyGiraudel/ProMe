import './PageCover.css'

/** Full-bleed decorative banner (Notion-style cover) above page content. */
export function PageCover() {
  return <div className='page-cover' role='presentation' aria-hidden='true' />
}
