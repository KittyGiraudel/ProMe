# Map style option

I have played with giving the map some perspective. It relies on a `data-style` attribute on the `.MapScroller` element in `MapDisplay.tsx`. We need to hook it up to a proper setting with 3 options: Flat, Tilted, Tilting on hover.
