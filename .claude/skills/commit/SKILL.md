## Commit Skill

1. Run `npx biome check --write .` to format
2. Run tests with `npm test` to verify nothing is broken
3. Review staged changes with `git diff --cached`
4. Write a concise conventional commit message (add context to the description if needed)
5. Commit the changes
